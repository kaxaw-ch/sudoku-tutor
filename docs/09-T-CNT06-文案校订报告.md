# 09 · T-CNT-06 教学文案校订报告

> 任务：教学文案生产与校订（P0-EDU-11、C-17）｜执行人：寇豆码（Kou）｜日期：2026-08-08
> 前置就绪：F-5 34 关 JSON、F-3 mistakes_zh.json、T-QA-05 回放校验（43 用例全绿）。

## 0. IS_PASS 结论

✅ **IS_PASS**。四项回归全绿；术语审计 0 不一致；mistakes 补足至 30 条（T-EDU-04 验收 20–30 条）；ARB 结构就位；报告齐备。

---

## 1. 扫描口径

- **权威基准**：`packages/sudoku_core/lib/src/techniques/technique_id.dart` 的 `TechniqueId.zhName`（定死译名，16 项）。
- **扫描范围**：
  - 34 关 JSON（`assets/curriculum/index.json` 登记的 34 个关卡）的 `title` / `intro` / `script.steps[].narration`；
  - `assets/text/mistakes_zh.json` 全文（含 `categories[].templates` 及 `prematureFill.templates` 的 `byTechnique` / `default` 全部字符串）。
- **检测项**：
  1. 技巧名：英文残留（`X-Wing`/`Swordfish`/`Naked Pair`…）、无空格变体（`X翼`/`XY翼`…）、中英混写括注（`裸对（naked pair）`）、非 zhName 核心名；
  2. 全半角标点混用（半角 `,`/`.`/`;`/`:`/`?`/`!`/`()` 出现在中文语境、半角双引号）；
  3. 坐标格式异形（非 `rXcY` 小写无分隔，如 `R3C5`、`r3-c5`）；
  4. mistakes 结构与 repo 解析兼容性（`byTechnique` 键合法、34 关教学技巧全覆盖、`default` 与 `byTechnique` 平级、模板占位符仅 `{cell}/{digit}/{technique}`）。
- **工具**：`app/tool/audit_terms.dart`（纯 Dart，可复跑）。已自证检出能力：临时向文案注入 `X-Wing`、`R3-C5`、半角逗号、`Naked Pair` 均被准确检出，恢复后 0 不一致。

## 2. 术语不一致清单

**发现 0 处，修复 0 处。**

34 关的 `title/intro/narration` 与 `mistakes_zh.json` 中出现的全部技巧名均为 zhName 核心名，未发现英文残留、无空格变体、全半角标点混用或坐标异形。F-5 产物本身已按 doc 03 §1 定死译名书写（如「裸对/隐对/裸三/隐三/X 翼/鳍形 X 翼/剑鱼/XY 翼/XYZ 翼/区块排除/唯一余数/隐性唯一数」），审计脚本确认逐字一致。

补充说明：标题中「鳍形 X 翼（实操一）」「剑鱼（演示）」等，其括注为关卡形态（演示/实操/试炼），非技巧名组成部分；技巧名核心「鳍形 X 翼」「剑鱼」与 zhName 核心一致，符合 doc 03 §1 与 build_curriculum.dart 既定标题风格，判为合规。

## 3. mistakes_zh.json 补足明细

### 3.1 结构调整（兼容 MistakeMessageRepository）

- `prematureFill.templates.default` **从 `byTechnique` 内部移到 `templates` 层**，与 `byTechnique` 平级——与 `MistakeMessageRepository._templatesOf` 的 `by['default']`（即 `templates['default']`）取用逻辑严格一致，修复了原结构下 default 兜底不可达的问题。
- `byTechnique` 键全部对齐 `TechniqueId.values.id`，覆盖任务书列出的 9 个教学技巧。

### 3.2 条数（任务书口径 19 → 验收口径 30）

按 `verify_mistakes.dart` 统计口径（wrongFill/deletedTrueCandidate 的 `templates` 条数 + prematureFill `byTechnique` 各组条数之和）：

| 类别 | 修复前 | 修复后 | 变化 |
|---|---|---|---|
| wrongFill | 9 | **11** | +2（补不同句式/格子类型） |
| deletedTrueCandidate | 9 | **10** | +1（补句式） |
| prematureFill.byTechnique | 7 组 | **9 组**（nakedPair/hiddenPair/nakedTriple/hiddenTriple/xWing/finnedXWing/swordfish/xyWing/xyzWing） | 补齐任务书要求的 9 个教学技巧 |
| **总计（验收口径）** | 29 | **30** | ✅ T-EDU-04 20–30 条验收达标 |

- 基础技巧（nakedSingle/hiddenSingle/lockedCandidates）的 prematureFill 由 `default`（4 条通用兜底模板）覆盖，符合 repo「byTechnique 无命中 → default」取用逻辑。
- 说明：任务书所述「当前 19 条」基于早期认知（当时 prematureFill 仅有 default 1 条）；实际 F-3 产物 prematureFill 已含 byTechnique 结构（29 条）。本批次在验收上限 30 条约束内完成 wrongFill/deleted 补足与教学技巧全覆盖，未突破 T-EDU-04 验收范围。

### 3.3 文案风格

- 两段式：`what`（错在哪）/ `how`（正确思路），**不给答案**（模板不透露正确数字）；
- 占位符仅使用 repo 支持的 `{cell}` / `{digit}` / `{technique}`（已由审计脚本校验）；
- 中文口语化，术语与 zhName 一致；prematureFill 模板以「{technique}」占位引用技巧名，由 repo 渲染为 zhName。

## 4. app_zh.arb 创建说明

- 路径：`app/lib/l10n/app_zh.arb`（新建）。
- 内容：`@@locale: "zh"` + **32 个核心 UI key**（appTitle/appSubtitle/学习地图/自由练习/设置/开始学习/跳过/即将推出/下一步/上一步/重播/退出笔记/重置进度/重置全部进度/返回章节/返回/取消/继续/放弃/重试/选择难度/音效/震动/隐私说明/导入成功/错题本已清空/错误次数/错在哪/这一步有问题/验收试炼/尚未解锁/引导语等），值直接使用简体中文。
- 说明：本期仅「简体中文结构就位」，**不接入 Flutter i18n 生成管线**（未建 l10n.yaml、未改 pubspec 的 gen-l10n），符合 doc 07 要求。

## 5. 改动文件清单

| 文件 | 类型 | 说明 |
|---|---|---|
| `app/assets/text/mistakes_zh.json` | 修改 | 补足 wrongFill/deleted 句式；prematureFill byTechnique 覆盖 9 教学技巧；default 移到 templates 层（repo 兼容） |
| `app/tool/audit_terms.dart` | 新建 | 术语/标点/坐标/mistakes 结构审计脚本（可复跑，纯 Dart） |
| `app/lib/l10n/app_zh.arb` | 新建 | 简体中文 ARB 结构就位（32 key） |
| `docs/09-T-CNT06-文案校订报告.md` | 新建 | 本报告 |

未改动：34 关 `assets/curriculum/*.json`（审计 0 不一致，无需修改）、`app/lib` 其余代码、`sudoku_core`、`sudoku_cli`、`app/test`。

## 6. 回归结果

| 验证项 | 结果 |
|---|---|
| `dart analyze tool/audit_terms.dart` | ✅ No issues found |
| `dart run tool/audit_terms.dart`（术语审计） | ✅ 0 不一致 |
| `dart verify_mistakes.dart`（mistakes 结构/条数） | ✅ 30 条，20–30 验收内 OK |
| `dart test tool/ci/verify_levels_test.dart`（T-QA-05） | ✅ **43/43 All tests passed** |
| mistakes_zh.json 解析 + 模板取用冒烟（按 repo 逻辑） | ✅ byTechnique 9 技巧全覆盖 + default 兜底可命中，占位符合法 |

文案改动仅涉及 mistakes 文本与结构，未触碰 34 关 `script.steps` 的 techniqueId/删数/填数，T-QA-05 43/43 保持全绿。

## 7. 遇到的问题与请求

1. **任务书条数基准与实际不符**：任务书写「当前 3 类 19 条（prematureFill 1 条）」，而实际 F-3 产物 prematureFill 已是 byTechnique 结构（验收口径 29 条）。本批次按验收口径（T-EDU-04 20–30 条）交付 30 条，wrongFill +2、deletedTrueCandidate +1、byTechnique 覆盖 9 教学技巧，均在验收范围内；若主理人希望 deleted 也 +2 条，需放宽验收上限至 31 条（不建议，会破坏 T-EDU-04）。
2. **prematureFill 基础技巧处理**：nakedSingle/hiddenSingle/lockedCandidates 未单独建 byTechnique 组，由 default 兜底——这是任务书「至少覆盖 9 个教学技巧」+「20–30 条上限」双重约束下的最优解；如后续 P1 阶段需要基础技巧专属文案，可再扩容。
3. **无其余阻塞**：34 关文案经审计确认无需修改，未触碰课程结构字段。
